{
  "name": "nx-deploy",
  "version": "1.0.1",
  "description": "",
  "main": "deployment.js",
  "bin": {
    "deploy": "./bin/deploy.js"
  },
  "scripts": {
    "preupload": "npm version patch",
    "upload": "npm publish",
    "postupload": "git push",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sample": "DEBUG=* deploy deploy -c sample/.deploy.yml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0",
    "debug": "^2.6.6",
    "deep-equal": "^1.0.1",
    "yamljs": "^0.2.10"
  }
}
